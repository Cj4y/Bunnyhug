<%= form_with(model: star_rating, local: true) do |form| %>
  <% if star_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(star_rating.errors.count, "error") %> prohibited this star_rating from being saved:</h2>

      <ul>
        <% star_rating.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
